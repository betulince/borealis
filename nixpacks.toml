[phases.setup]
nixPkgs = ['jdk17']

[phases.install]
cmds = ['chmod +x gradlew']

[phases.build]
cmds = [
  'export JAVA_HOME=$(dirname $(dirname $(which java)))',
  './gradlew clean bootJar --no-daemon'
]

[start]
cmd = 'java -Dserver.port=$PORT -jar build/libs/borealis-0.0.1-SNAPSHOT.jar'
